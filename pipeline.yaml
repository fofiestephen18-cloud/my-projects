#key: value 
name: My pipeline

# nesting = indentation (spaces, never tabs)
jobs:
  build:
    runs-on: ubuntu-latest

    # a list uses dashes 
    steps:
      - name: Say hello 
        run: echo "hello"